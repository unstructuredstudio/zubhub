# from django.db.models.signals import pre_save
# from django.dispatch import receiver
# from .models import Creator, ActivityLog

# @receiver(pre_save, sender=Creator)
# def pre_edit_account(sender, instance, **kwargs):
#     oldValues = Creator.objects.get(pk=instance.pk)
